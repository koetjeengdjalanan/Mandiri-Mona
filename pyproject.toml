[project]
name = "mandiri-mona"
version = "0.2.0"
description = "An Helper Automation tools for logstash"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    {name = "Martin Sambulare", email = "martin.sambulare@nttindonesia.com"},
]
dependencies = [
    "influxdb-client>=1.50.0",
    "netmiko>=4.6.0",
    "pydantic>=2.12.4",
    "python-dotenv>=1.2.1",
    "rich>=14.2.0",
    "tenacity>=9.1.2",
]

[dependency-groups]
code = ["black>=25.9.0", "isort>=6.0.1", "ruff>=0.13.2"]
dev = [
    "ipykernel>=6.29.4",
    "ipython>=8.23.0",
    "pytest>=8.3.0",
    "pytest-cov>=5.0.0",
    "pytest-mock>=3.15.1",
]

[tool.black]
line-length = 120
target-version = ['py312']
include = '\.pyi?$'

[tool.ruff]
line-length = 120
target-version = "py312"
exclude = [
    "debug.py",
    "build",
    "dist",
    "__pycache__",
    ".venv",
    "venv",
    "*.pyc",
    "*.pyo",
    "*.pyd",
    "*.so",
    "*.dylib",
    "*.dll",
]

select = ["E", "W", "F", "I", "D"]
ignore = ["D212", "D107"]

[tool.ruff.per-file-ignores]
"__init__.py" = ["F401", "D104"]
"debug*.py" = ["ALL"]

[tool.ruff.pydocstyle]
convention = "google"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -vv --cov=libs --cov=main --cov-report=term-missing"
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
